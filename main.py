LEDs: neopixel.Strip = None
rotate = 0
strip = neopixel.create(DigitalPin.P13, 14, NeoPixelMode.RGB)
pins.set_pull(DigitalPin.P11, PinPullMode.PULL_UP)

def on_forever():
    global rotate, LEDs
    rotate = pins.digital_read_pin(DigitalPin.P11)
    if rotate == 0:
        strip.show_color(neopixel.colors(NeoPixelColors.BLACK))
        strip.show()
    else:
        LEDs = neopixel.create(DigitalPin.P13, 16, NeoPixelMode.RGB)
        LEDs.set_brightness(100)
        LEDs.set_pixel_color(0, neopixel.rgb(255, 255, 255))
        LEDs.shift(1)
        LEDs.show()
        basic.pause(1000)
        LEDs.set_pixel_color(1, neopixel.rgb(255, 255, 255))
        LEDs.shift(1)
        LEDs.show()
        basic.pause(1000)
        LEDs.set_pixel_color(2, neopixel.rgb(255, 255, 255))
        LEDs.shift(1)
        LEDs.show()
        basic.pause(1000)
        LEDs.set_pixel_color(3, neopixel.rgb(255, 255, 255))
        LEDs.shift(1)
        LEDs.show()
        basic.pause(1000)
        LEDs.set_pixel_color(4, neopixel.rgb(255, 255, 255))
        LEDs.shift(1)
        LEDs.show()
        basic.pause(1000)
        LEDs.set_pixel_color(5, neopixel.rgb(255, 255, 255))
        LEDs.shift(1)
        LEDs.show()
        basic.pause(1000)
        LEDs.set_pixel_color(6, neopixel.rgb(255, 255, 255))
        LEDs.shift(1)
        LEDs.show()
        basic.pause(1000)
        LEDs.set_pixel_color(7, neopixel.rgb(255, 255, 255))
        LEDs.shift(1)
        LEDs.show()
        basic.pause(1000)
        LEDs.set_pixel_color(8, neopixel.rgb(255, 255, 255))
        LEDs.shift(1)
        LEDs.show()
        basic.pause(1000)
        LEDs.set_pixel_color(9, neopixel.rgb(255, 255, 255))
        LEDs.shift(1)
        LEDs.show()
        basic.pause(1000)
        LEDs.set_pixel_color(10, neopixel.rgb(255, 255, 255))
        LEDs.shift(1)
        LEDs.show()
        basic.pause(1000)
        LEDs.set_pixel_color(11, neopixel.rgb(255, 255, 255))
        LEDs.shift(1)
        LEDs.show()
        basic.pause(1000)
        LEDs.set_pixel_color(12, neopixel.rgb(255, 255, 255))
        LEDs.shift(1)
        LEDs.show()
        basic.pause(1000)
        LEDs.set_pixel_color(13, neopixel.rgb(255, 255, 255))
        LEDs.shift(1)
        LEDs.show()
        basic.pause(1000)
        LEDs.set_pixel_color(14, neopixel.rgb(255, 255, 255))
        LEDs.shift(1)
        LEDs.show()
        basic.pause(1000)
        LEDs.set_pixel_color(14, neopixel.rgb(255, 255, 255))
        LEDs.shift(1)
        LEDs.show()
        basic.pause(1000)
basic.forever(on_forever)

